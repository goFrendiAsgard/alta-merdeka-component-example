package controller

import (
	"testing"
)

func TestAuthorControllerGetAll(t *testing.T) {
	// db, err := gorm.Open(sqlite.Open("sqlite:///.db"), &gorm.Config{})
	// if err != nil {
	// 	t.Error(err)
	// }
	// authorController := NewAuthorController(db)
	// authorController.GetAll(c)
}
